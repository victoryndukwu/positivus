export type Service = {
  title: string;
  image: string;
  variant: "light" | "primary" | "dark";
};

export type TeamMember = {
  name: string;
  position: string;
  image: string;
  description: string;
};
